{
  "name": "migration",
  "version": "1.0.0",
  "description": "The games must be served over HTTP (opening the HTML files directly with `file://` can break loading and sounds).",
  "main": "index.js",
  "scripts": {
    "start": "sh run-server.sh",
    "deploy": "sh run-server.sh",
    "serve": "sh run-server.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
